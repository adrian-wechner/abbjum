<%= render partial: "layouts/callouts" %>

<div class="container mt-3 mb-3">
  <%= render @product %>


  <div class="mt-3">
    <a href="<%=products_path%>" class="btn btn-primary">Regresar a Productos</a>
    <a href="<%=edit_product_path(@product)%>" class="btn btn-info">Editar</a>
  </div>
</div>
