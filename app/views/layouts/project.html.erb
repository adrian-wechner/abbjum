<!DOCTYPE html>
<html>
  <head>
    <title>ABB/Jumulus MES</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <link rel="icon" type="image/svg" href="/favicon.svg">

    
  </head>

  <body>
    <script src="/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>

    <%= yield :corner %>

    <%= yield %>

    <% if controller.controller_name == "abb" && controller.action_name == "project" %>  
    <script>
      setTimeout(function() {
        location.reload();
      }, 60000); // reload page after x seconds
    </script>
    <% end %>

  </body>
</html>
